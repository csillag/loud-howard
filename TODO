
Absolutely:

- Fix the XPath dialect problem (and any other format inconsistencies)
- Fix the FF incompatibility problem
- Make a "production" installation, which serves minified JS files.
- Add support for generating threads
- Port dom-text-matcher to the new dom-text-mapper interface
- Port dom-text-mapper-demo to the the new set of libraries
    - different set of files
    - new iface of dtHiliter
    - new iface of dtMatcher ?

Later:

- Clean up the code of the tool.
- Annotation-store: add an API on the backend that supports saving several annotations in one step.
- Improve session timeout handling
   - false positive (when trying to navigate via dead proxy)
   - false negative (whan can't load app))
